# Implementation Plan

1. Run the one-command operator smoke runner with env-only credentials.
2. Inspect runner summary and nested smoke report.
3. Add a sanitized verification doc under `docs/`.
4. Run a lightweight doc/sanitization check, archive, and commit.
