# Readiness Next Action Center Implementation Plan

1. Extend readiness summary with action helpers and `primary_action`.
2. Update dashboard JS to render primary and per-recommendation actions.
3. Add CSS for recommendation action layout and QA readiness callout.
4. Add/update i18n strings.
5. Add unit tests for action fields and static asset wiring.
6. Run JS syntax checks, py_compile, targeted pytest, browser smoke, and `git diff --check`.
