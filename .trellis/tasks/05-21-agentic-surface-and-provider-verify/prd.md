# Agentic Surface and Provider Verify

## Goal

AgenticConfig + AgenticDecisionConfig wired into Settings; per-request override on Search/Retrieve/Answer requests; eval/replay --force-mode CLI; production_provider_verify decision step (D5). Depends on all flavor children.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
